# Backend API Configuration
API_URL=http://localhost:8000/api

# PostgreSQL (optional — backend panorama list + metadata)
# DATABASE_URL=postgresql+psycopg2://interior:interior@localhost:5432/interior_design
# BACKEND_PUBLIC_URL=http://192.168.x.x:8000

# AWS S3 Configuration (if direct upload is needed)
# AWS_REGION=us-east-1
# AWS_BUCKET_NAME=your-bucket-name

# API Keys (for future use)
# NANOBANANA_API_KEY=your-api-key
# WORLDLABS_API_KEY=your-api-key
